# Port the Express server will listen on during local development.
PORT=5000

# MongoDB connection string. Replace this with a local MongoDB URI or MongoDB Atlas URI.
MONGO_URI=mongodb://localhost:27017/shoppyglobe

# Secret key used to sign and verify JWT tokens. Replace this with a long random string.
JWT_SECRET=replace_this_with_a_long_random_secret
